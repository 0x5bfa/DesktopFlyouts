#pragma once

// The native XAML compiler emits an unconditional include of "pch.h" for
// XamlMetaDataProvider.cpp. This is only a compatibility header; the project
// does not compile or use a precompiled header (/Yc or /Yu).
